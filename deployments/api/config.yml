name: ex-api-dev
port: 8000

environment:
  ENV_NAME: "development"
  LOG_LEVEL: "debug"

commands:
  - "uvicorn app:app --host 0.0.0.0 --port 8000"

dependencies:
  pypi:
    fastapi: ""
    uvicorn: ""

resources:
  cpu: "0.5"
  memory: "512Mi"